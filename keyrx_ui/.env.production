# Production Environment Configuration
# KeyRx Daemon API and WebSocket URLs

# API Base URL - production daemon (same host as UI)
VITE_API_URL=

# WebSocket URL - production daemon (same host as UI)
VITE_WS_URL=

# Disable debug logging in production
VITE_DEBUG=false

# Production mode flag
VITE_ENV=production
