# KeyRx Error Codes

KeyRx uses structured error codes in the format `KRX-CXXX` where:

- `C` is the category prefix (single letter)
- `XXX` is a three-digit error number

Each category has a dedicated range of error codes:

