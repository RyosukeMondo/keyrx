//! Extended state management module (re-exported from runtime)
//!
//! This is a re-export of the runtime state module for convenience.
//! The actual implementation is in `runtime::state`.

pub use crate::runtime::state::*;
